---
status: accepted
---

# RPC 独立启动，QMT 在后台连接

RPC 独立启动，QMT 首次连接及后续重连在进程内后台执行，使券商维护或交易连接失败不妨碍认证、契约协商和健康查询。

不采用首次同步尝试后降级启动，因为原生连接阻塞仍会阻止 RPC 启动；暂不采用 SDK 子进程隔离，以控制接口转发、回调及资源管理复杂度。后台线程不提供原生崩溃隔离，也不能强制终止永久阻塞的 SDK 调用。运行行为和限制见[架构](../design/architecture.md#启动安全与验收)。
