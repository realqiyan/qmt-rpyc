# 单一类型化公共契约与可替换来源

公共名称、请求、返回模型由按业务组织的纯 Python 契约定义；客户端、RPC 传输、服务端任务调度和 SDK 适配器各自独立。协议版本仅用于协商，不作为包路径或客户端类名；公开 API 不直接投射 SDK 的动态接口面，以便替换来源而不改变使用方的数据语义。

请求与结果模型放在同一能力模块，采用冻结 dataclass 和严格 JSON 编解码。服务端只在 RPC 边界进行字符串操作路由，内部面向类型化 Providers；不采用每个操作一个包或提前拆分多个发行包。详见[架构](../design/architecture.md)。
