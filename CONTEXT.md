# QMT 数据与交易

本词汇表区分券商维护期间仍可提供的历史事实，与需要外部连接才能更新的市场及账户事实，也区分券商 SDK 接口与客户端依赖的桥接 API 约定。

## Language

**部署 SDK 接口面**：某一实际券商部署的 xtquant 所提供的函数、方法、常量及其签名。它描述该部署提供了什么，不代表所有接口已经通过远程传输或业务行为验证。

**桥接 API 契约**：qmt-rpyc 向客户端承诺的接口名称、输入和输出约定。它与部署 SDK 接口面是不同的概念；契约的具体基线及兼容范围须明确约定。

**业务能力保留**：保留使用方能完成的业务任务及其必要信息和行为，不要求沿用旧接口名称、签名或数据容器。

**使用方必需接口**：a-trader 或 a-options 的实际使用路径依赖的桥接接口，包含其所需的输入、返回字段及调用行为。它们构成固定契约必须保障的范围，不能仅凭使用方声明的接口名单判断是否需要。

**委托编号**：QMT 用于标识委托的编号，同步下单结果提供该编号。它与柜台合同编号是两种不同的标识，撤单时须使用对应的编号。

**柜台合同编号**：柜台侧的委托标识，按该编号撤单还需要明确交易市场。它不是账户号，也不能与委托编号互换。

**QMT 本地数据**：QMT/SDK 已在服务端本机保存的数据。其存在不代表覆盖了本次查询范围，也不代表数据仍是最新的。

**下载任务完成**：SDK 下载调用已正常结束。它不代表请求范围内的数据已经完整，也不保证使用方所需的数据均可读取。

**交易连接恢复**：Trader 重新连接成功，且已配置账户时该账户订阅也成功。该状态不保证具体交易请求会被券商接受。

**历史行情**：某一过去时间区间内的市场价格、成交量等记录。其可用性取决于该区间的数据覆盖，不代表当前市场状态。

**最新行情**：行情源最近提供的市场观测，必须结合观测时间判断新鲜度。上一交易日收盘价属于历史行情，不能仅因它是本地最后一条记录就称为实时行情。

**账户快照**：在明确时刻从券商取得的余额、持仓、委托等账户事实。断线期间保留的快照不代表账户当前状态。

**期权合约档案**：期权合约的标的、认购认沽方向、行权价、到期日、乘数及调整历史，包含已到期合约。历史分析需要对应历史时点的合约属性。

**当前期权发现**：获取标的当前尚未到期合约的到期日及合约集合。它不承诺查询历史时点或已到期的合约目录，也不等于历史期权合约档案。

**提交结果未知**：提交可能已在券商侧执行，但调用方未取得可信结果。它不等于明确拒绝，也不同于查询到一笔委托但无法解释其当前状态。

**回测数据集**：一次回测使用的确定版本的历史事实集合，包括所需行情及其日历、复权和合约资料。完整性以该次回测声明的需求为准。
